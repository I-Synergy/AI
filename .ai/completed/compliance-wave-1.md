# Compliance Wave 1 — ISO 25010 / 29119-3 / Traceability

**Status:** DONE — 2026-09-25
Started: 2026-09-25

All gates green on the final tree: 14/14 suites, 16/16 pytest, hygiene 0 issues, standards checker
0 failures / 0 warnings, all reference tests passed. 42 files modified, 14 created
(+983 / −160).

Plan: `.ai/plans/compliance-wave-1.md`
Slug: compliance-wave-1

## Steps

- [x] Step 1 — ID convention: `.ai/reference/traceability.md` (artifact 1)
- [x] Step 2 — Apply scheme to skills 2-5 + pattern 8, templates 6-7
- [x] Step 3 — `validate-traceability.py` + registration 9-13
- [x] Step 4a — ISO/IEC 25010 quality model (artifacts 14-18)
- [x] Step 4b — ISO/IEC/IEEE 29119-3 test documentation (artifacts 19-24)
- [x] Step 5 — Standards-compatibility surface (25-33)
- [x] Step 6 — Propagation (34-38)
- [x] Step 7 — Run `.ai/tests/run-all-tests.sh`, read output — 14/14 suites, 16/16 pytest, hygiene 0
- [x] Step 8 — `reviewer` audit of the whole change set — 9 findings, all 9 dispositions verified
      implemented by an independent pass; the 7 open claims answered in the results section
- [x] Step 9 — Move this file to `.ai/completed/compliance-wave-1.md`

## Notes

- Step 1 gates everything — nothing else starts until `traceability.md` exists.
- `.ai/session-context.md` is **not** written (plan R8): it ships as the `CUSTOMIZE THIS`
  placeholder in the template repo.
- The DeepSeek profile swap is live in the working tree (`CLAUDE.md` ≡ `DEEPSEEK.md`,
  `CLAUDE.md.deepseek-backup` present). Root-doc edits in Step 6 must target the content
  that survives the restore, and land in both files in the same commit (plan R2).
- New `.ai/reference/*.md` files need explicit `TEMPLATE_OWNED` entries (artifact 33).

## Rulings made during execution

- **Step 1:** `@UC-` tags on main-flow scenarios **rejected** — the untagged link is UC → Gherkin
  (documentation), while UC → slice → test is already machine-checkable via the blueprint `source`.
  Recorded as a supported gap in `traceability.md`.
- **Step 1:** "mixed" documents (some criteria labelled) are **advisory only**, same rationale as T7.
- **Step 1:** T6 — a tagged *executable* Reqnroll scenario counts as satisfying "≥1 test". A
  TestCategory-only implementation would emit spurious WARNs on every BDD-covered criterion.
- **Step 1:** blueprint `source.acceptance_criteria` **replaces** the top-level free-text array
  (IDs only; Gherkin lives once, tagged, in the `.feature`), with legacy tolerance for the old
  singular keys.
- **Step 2:** IDs are allocated **at first write**, not withheld until finalization.
- **Step 2:** legacy blueprints are parsed tolerantly and converted on next touch — never
  bulk-rewritten.
- **Step 2:** `alternate_flows` key **not added** to `source` — nothing in Wave 1 would check it.
  Carried in the plan's Open Items (#6) for Wave 2.
- **Step 2:** a test that is evidence for **no** criterion carries no `AC-` category and sits outside
  the trace. `traceability.md` clarified; the two non-evidence methods in `test-class.cs.txt` now
  carry `[TestCategory("Unit")]`. Omission is still caught — an untagged test for a real criterion
  leaves that criterion uncovered and T6 warns.
- **Step 2:** the `tester`'s unrequested fix to the source doc's own Quick Checklist (`:488`) is
  **approved** — the clarification ordered above created that contradiction, so fixing it is part of
  the same change, not scope creep.
- **Step 2 correction to the plan's premise:** the plan (and the survey behind it) called the
  `testing-patterns.md` block a "verbatim duplicate" of `feature-file.feature.txt`. It was **not** —
  the pattern was missing the pagination scenario (4 vs 5). Reconciled toward the richer template.

## Step status

- **Step 1 — DONE.** `.ai/reference/traceability.md` (untracked/new). 11/11 suites.
- **Step 2 — DONE.** Four skills + two templates + `testing-patterns.md`. 11/11 suites,
  `hygiene-lint` 0 issues, duplicate blocks verified byte-identical after renumbering.

## Outstanding nits

- **Artifact-location handoff for Step 5.** The 29119-3 work had to name locations for its new
  artifacts since none existed. Artifact 25 (`.ai/reference/standards.md`, `architect`) **must use
  these same paths** or one of the two documents has to move:
  - Test plan → `docs/slices/{BC}/{Entity}.{Operation}/test-plan.md` (per slice, from `blueprint.json`)
  - Test completion report → `docs/test-reports/{release}-test-completion-report.md` (per release)
  - Test incident report → `docs/test-reports/incidents/TIR-{yyyy-mm-dd}-{NN}.md` (one per failure)
  - Test case specification → **no file**; generated by the traceability mapping (`traceability.md`)
- **Plan correction — the item count.** The plan asserted "17 information items / 13 omitted". The
  enumerable total is **16** (Clause 6: 2, Clause 7: 3, Clause 8: 11), so it is 4 adopted /
  **12 omitted**. `test-documentation.md` records the discrepancy rather than padding to 17. The
  plan is corrected; **do not let `standards.md` or `COMPLIANCE.md` restate 17.**
- **Annex A conformance levels are not cited anywhere.** `test-documentation.md` could not source
  them, so omission reasons are stated as this template's own right-sizing decisions rather than as
  claims about what the standard permits. Keep it that way downstream.

- ~~`traceability.md` enforcement table (`:278`) says "A **story** document with zero identifiers"
  while the Legacy Mode prose (`:303`) says "A **bounded-context** document".~~ **[DONE]** The table
  now reads "A story or use-case document with zero identifiers", and "bounded-context document"
  occurs nowhere in the file — verified by grep in the Step 8 verification pass.
- ~~**Same-defect cleanup — plan artifacts 39-41**~~ **[DONE]**
  The forbidden extension-method pattern ran deeper than plan R7 assumed. **Seven sites** across five
  files, all fixed:
  `dotnet-engineer/SKILL.md:36,50,54` (**the primary C# skill — it instructed agents to use the
  pattern rule 2 forbids**, so any solution built from it violated rule 2 on every delete path),
  `api-security/SKILL.md:233` (a working sample — fixed the code *and* added the missing null check,
  which would otherwise produce CS8602 under the mandated nullable settings), `technical-writer/SKILL.md:161`
  (a Mermaid label), `unit-tester/SKILL.md` (3 foreign paths), `glossary.md:18` (a prescriptive
  "Use DataContext extensions instead" in the glossary's own prescription column).
- **[DONE] Foreign absolute paths** — `dotnet-engineer/SKILL.md:58-65` pointed at five templates and
  patterns via `/d/Projects/Template/.ai/…`, **another machine's path**. An agent following that skill
  could not read the files it was told to use, and nothing caught it because the paths were plain text
  rather than backticked or linked. Rewritten repo-relative, all five targets verified on disk. A full
  `.ai/` sweep found no other foreign path; the remaining absolute-path hits are legitimate (Git Bash
  system paths, `~/.claude/` global references, Hugo upstream theme assets describing their authors'
  machines, and a `--target` placeholder in a CLI example the user supplies).
- **Lesson for future sweeps:** `GetItemByIdAsync` does **not** contain `ItemAsync` as a contiguous
  substring (it is `Item` + `ById` + `Async`), so a grep for `ItemAsync` alone silently misses one of
  the exact defect forms. Grep both.
- ~~**Legacy-mode table wording** in `traceability.md` (`:278` says "story document", `:303` says
  "bounded-context document")~~ **[DONE]** — closed in the same edit pass; see the entry above.
- **Orphan-warning caveat (corrected in the Step 8 verification pass):** `validate-claude-md.py`'s
  two orphan warnings are **structural, not a swap symptom**. The checks look for *skill* and
  *pattern* references the root docs never enumerate — `CLAUDE.md`'s table names agents, not skills
  — and both warnings persist on the restored neutral master. Observed sets: skills incl.
  `api-endpoints`, `api-security`, `architect`, `blazor-specialist`; patterns incl.
  `microservices.md`, `mvvm.md`, `object-oriented-programming.md`,
  `service-oriented-architecture.md`. The wave never changed which skills or patterns a root doc
  references, so nothing about the swap could have moved this set.

## Evidence recorded (not claims — observed output)

**V9, the honesty gate — SATISFIED with causation demonstrated.** Run in a scratch copy of the tree,
never the repo:

| State | traceability exit | standards exit | 29119-3 row |
|---|---|---|---|
| Clean | 0 | 0 | `✅ … Evidence (as the manifest declares)`, `Rows downgraded: 0` |
| Undeclared `@AC-Budget-014.1` planted in a fixture `.feature` | 1 | 1 | `❌ FAIL … declared 'Evidence', computed 'Aligned' (mandatory note: downgraded: proving validator … exited 1)`, `Rows downgraded: 1` |
| `.ai/reference/test-documentation.md` hidden | 0 | 1 | downgraded: `implementing artifact … is missing` |
| Fixture removed | 0 | 0 | back to `Evidence` |

Two independent causes, both with restores verified. **The status column cannot claim more than the
validators proved** — which is the entire thesis of the compatibility surface, now demonstrated
rather than asserted.

**Counts at Step 5 close:** `run-all-tests.sh` 13 suites / 13 passed; `test_suite.py` 15 collected /
15 passed; CI 13 validation steps; `validate-structure.sh` `reference_files` 10, `template_files` 10;
`upgrade-template.py` `TEMPLATE_OWNED` 28, `PROJECT_OWNED` 9. Ownership integration test green —
including that a downstream `COMPLIANCE.md` survives an upgrade untouched.

**Root-doc verification (mine, independent of the writing agent's report):**
`CLAUDE.md.deepseek-backup` 187 lines, pure CRLF, 25 headings; `DEEPSEEK.md` 189 lines, **pure LF**,
25 headings; heading lists identical. +4 lines each, consistent with four Reference Appendix rows.
The writing agent's stated *starting* count was wrong but the final state is correct — verified
rather than accepted.

## Step 8 — audit findings and disposition

Independent audit (`reviewer`, read-only) found **9 findings**. The priority-1 sweep — the four
skills' and two templates' emitted formats against `traceability.md`, the largest unverified
integration in the wave — came back **clean**: every vehicle matches, including the duplicated
feature block.

| # | Sev | Finding | Disposition |
|---|---|---|---|
| 1 | Major | `standards.md:174` claims `validate-traceability.py` resolves `US-`/`UC-` refs; `:435` **discards** the use-case list and no `Ref` is ever built for them, so `"user_stories": ["US-Budget-999"]` is a silent typo — advertised as checked on the row carrying the wave's only `Evidence` claim | **Implement the check** (not narrow the claim): ID-shaped `US-`/`UC-` values must resolve, non-ID-shaped stay advisory-legacy. Also enforce `traceability.md:221` (a `source` naming neither key is untraceable) |
| 2 | Major | The T1–T4 fixture regression **never runs in CI** — `run-all-tests.sh` doesn't invoke pytest and no workflow step does. Step 5b was added to stop the validator rotting, and was placed where CI can't see it. The plan even asserted "CI runs pytest separately" | Add a pytest step to the workflow; correct the plan's stale V2 parenthetical |
| 3 | Major | The working-tree `CLAUDE.md` is a **pre-wave** copy, so V4 was never exercised against the file that would be committed; `.gitignore:13` keeps the neutral master's copy out of version control until the swap restores | **Operational** — see the standing warning: do not commit in-session; re-run `validate-claude-md.py` after the restore |
| 4 | Major | `Evidence` is defined as "followable to a file", but the downgrade tests only `Implementing artifact`; row 1's `Evidence a project produces` paths are `{…}` patterns skipped by design — so a project with a `.sln` and no test plan still renders `Evidence`, naming three files it does not contain | **Make an `Evidence` row conditional on ≥1 concrete instance existing** (glob the patterns; downgrade with a note otherwise). Update `verify-config` step 6 to match |
| 5 | Minor | T7's stem heuristic (`stor`/`use-case`/…) treats `history.md` and `storage-notes.md` as story documents, and exempts `glossary.md` though the spec says every bounded-context document is in scope | Tighten the match; state the real scope in `traceability.md` |
| 6 | Minor | A padded `{n}` (`AC-Budget-014.02`) passes silently at every severity, while `traceability.md:49` says `{n}` is unpadded — a project can drift permanently into an undefined form and report clean | Keep tolerant resolution, **add an advisory WARN** on padded `.n` |
| 7 | Minor | `TESTING-GUIDE.md` and `QUICK-REFERENCE.md` list only pre-wave suites — a developer following them cannot find the three new validators | Add the suites, or point at `.ai/tests/README.md` |
| 8 | Minor | `test-documentation.md:13` asserts Annex A's conformance scheme while `:160-163` of the same file says Annex A was unobtainable — and `standards.md:245-247` promises no conformance scheme is cited. `:96` states Clause 7.4's outline unhedged, unlike the file's treatment of Annex E and Annex S | Reword both to match the file's own standard |
| 9 | Minor | `test-plan.md.txt` names `{Operation}{Entity}CommandHandlerTests`; `test-class.cs.txt:1,12` uses `Create{Entity}HandlerTests` and `:17,30` declares `Create{Entity}Handler` — **violating rule 5's required `Command` suffix**. A test plan therefore names a class the generated project lacks | Align all sites on the rule-5 form that `traceability.md:238` already uses |

**Findings 1, 4 and 8 are over-claims inside a wave whose thesis is that it makes none** — that is
the most important result of the audit, and the reason to re-run the gates after the fixes rather
than accept the first green run.

**V9 verdict from the audit** (reached by *reading the code path*, not the recorded output): holds.
No path yields `Evidence` from a red, missing, timed-out or unrunnable validator; the fail-safe
direction is taken in every error mode; a stale surface token cannot escape the provenance equality
check. The real limits are finding 2 (nothing runs the fixture in CI) and the fact that
`.github/workflows/` is not template-owned, so downstream the guarantee is "the recorded exits are
checkable", not "the file is currently true".

**Consciously accepted, not overlooked.** `.ai/reference/standards.md:11` describes `Evidence` as
"an artifact a certification auditor can sample directly". That is definitional — it says what the
value is *for* — and hedging it into the template's own opinion would make the definition circular
("Evidence means an artifact this template treats as evidence"). Everywhere else the same
auditor-practice phrasing was aligned to the hedged form the completion-report template now uses;
this one instance is a decision to keep. If it should go, it needs a better definition, not a hedge.

**Every verification layer caught something the one above it missed** — worth recording because it
is why the review step exists rather than being ceremony: the plan asserted 17 ISO items without
counting (an agent counted 16 and declined to invent a row); called a duplicated block "verbatim"
(an agent measured it and found a missing scenario); marked the parity guard optional (an agent
noted a guard that never runs is worse than none); and finally the audit found over-claims *inside
the mechanism built to prevent over-claims*.

## For the Step 8 reviewer

Claims that matter and would be costly to discover later are wrong. Verify these independently —
do not take the build notes as evidence:

1. **`validate-traceability.py` accepts `AC-Budget-014.02`.** `.{n}` is matched as `\d+`, so a
   zero-padded child number passes, though `traceability.md`'s Format section says `{n}` is
   unpadded. The implementer's argument is that tightening would silently drop the *declaration*
   (nothing warns about a malformed id on a checkbox line) and turn a formatting slip into a
   coverage hole. Decide whether tolerance or literal conformance is right here.
2. **T3 resolves only `source.acceptance_criteria`.** A blueprint naming a non-existent `US-…` or
   `UC-…` produces no finding. Deliberate under-implementation, flagged for Wave 2 — confirm it is
   recorded as a gap rather than merely absent.
   **Verdict (Step 8 verification): the claim was stale and there is no gap.** Finding 1's
   disposition was carried out — refs are now built for `source.user_stories` *and*
   `source.use_cases` — see the results section.
3. **T7 selects documents by stem substring** (`stor`, `use-case`, `use_case`, `usecase`). A
   false-positive here would be a spurious advisory warning only, but substring matching on
   filenames is the kind of heuristic that surprises a later reader. Check it against a plausible
   filename that is not a story document.
4. **V9 — the honesty gate, and the reason Step 5b exists.** Force `validate-traceability.py` to
   fail, then confirm `validate-standards.py` fails and the generated status downgrades. If a red
   validator can still yield an "Evidence" claim, the mechanism is broken and the whole
   compatibility view is decoration.
5. **V8 — root-doc parity.** Every new section heading present in *both* `CLAUDE.md` and
   `DEEPSEEK.md` — **not** byte equality (R2; the two files differ deliberately at HEAD). The swapped
   working tree noted here was repaired *before* this verification pass: `CLAUDE.md` is the neutral
   master again (title `:1`, identity `:5`) and `DEEPSEEK.md` carries the variant.
6. **The manifest's parsing contract matches `validate-standards.py`.** The manifest documents how it
   is parsed; the checker implements that. Two agents wrote them from the same brief — confirm they
   agree rather than assuming they do.
7. **Legacy-mode wording nit** in `traceability.md` (`:278` "story document" vs `:303`
   "bounded-context document") — still open.

## Step 8 verification — results (independent pass, 2026-09-25; gates re-confirmed 2026-09-26)

Two independent passes ran against the frozen tree: an **empirical** one that built minimal fixtures
under `docs/` (a path this repo does not ship), exercised the validators and then removed every
fixture — the tree was restored byte-identical, `git status` diffing clean against the pre-experiment
capture — and a **read-only documentary** one. Neither took the build notes as evidence.

1. **Padded `{n}` — CONFIRMED.** A padded child number fires an advisory on the declaration *and* on
   each of the three reference carriers (4 warnings), the exit code stays `0`, and the criterion
   stays declared with every reference resolved — coverage is not lost, which was the implementer's
   argument for tolerance. The unpadded control is silent. `validate-traceability.py:69`,
   `:596-613`.
2. **T3 under-implementation — the claim was stale; no gap exists.** See the annotation above.
3. **T7 stem heuristic — PARTIAL.** The story stem is boundary-anchored
   (`validate-traceability.py:112-115`): `history.md`, `storage-notes.md` and `restoration-notes.md`
   are silent while the positive controls still warn. The use-case branch (`:314-316`) keeps a bare
   substring, so `use-cases-migration-log.md` is selected and warned about. Advisory-only, and the
   name genuinely *starts* with `use-cases`, so no boundary rule separates a spec from a log here.
   Recorded below as a stated limit rather than fixed.
4. **V9 — CONFIRMED on TEST 5 and TEST 6, the half this tree can exercise.** A planted undeclared
   `@AC-` tag made `validate-traceability.py` exit `1`; `validate-standards.py` then exited `1`, the
   computed status moved `Evidence` → `Aligned` with the mandatory note, and the declared/computed
   mismatch was the FAIL. One wording correction to the record: the manifest is never rewritten —
   the *computed* token downgrades and the disagreement is the failure, which is a stronger property
   than "the status downgrades". TEST 7's surface/provenance equality is verified by reading only:
   this repo ships no `COMPLIANCE.md` and README carries no status markers, so no fixture under
   `docs/` could exercise it.
5. **V8 — HOLDS, with a stated blind spot.** 8/8 level-2 headings match in both files, in order, and
   all 18 level-3 headings happen to match as well — but the check compares level 2 only, as its own
   module docstring says. The four new Reference Appendix rows could have been added to one file
   alone and parity would still pass, which is precisely the R2 failure mode the guard exists to
   prevent. That is a limit, not a defect: it guards structural drift, and structural drift is what
   the swap causes. The level-1 title and identity lines are likewise uncompared, which is correct —
   they differ deliberately.
6. **Manifest ↔ checker — one real disagreement, now fixed.** The checker implements three downgrade
   causes; rule 6 named two, and the manifest is the contract both generator and checker must read
   identically. Rule 6 now names the third cause, rule 4's "never checked for existence" is narrowed
   to say what the exception asks, and `validate-standards.py:456-464`'s comment is true as written
   without editing the script. The disagreement was always in the safe direction — a downgrade, never
   an over-claim.
7. **`traceability.md` `:278`/`:303` wording nit — FIXED.** See the entries above.

**Also fixed in this pass:** README's `reviewer` row was the one lagging copy of the root docs' agent
table (no validator compares it); and `traceability.md` now states the blueprint's silent tolerance
for a `source` value that names no identifier — finding 1's "advisory-legacy" wording was implemented
as silence, with the code comments as its only record.

**Deferred to Wave 2, stated rather than dropped:** the use-case stem's bare substring (advisory-only
false positive); V8's level-2-only comparison; and the advisory WARNs printing beneath per-test
headers labelled `-- FAIL` — cosmetic, since the exit code and the summary line are both correct, but
a reader of the raw output sees `FAIL` above `Failures: 0`.

**Gates re-run after the fixes landed:** `run-all-tests.sh` 14/14 suites, pytest 16/16,
`hygiene-lint.py` 0 issues, `validate-standards.py` 0 failures / 0 warnings (17 path claims, 1
validator run, 3 rule-9 applicability notices).

**Post-commit, 2026-09-26 — `.slnx`.** Rule 9's applicability test matched `*.sln` only, so a
project on the XML solution format read as "no solution": `verify-config` would have reported a
no-op and never generated `COMPLIANCE.md` or the README section. The repo already disagreed with
itself here — `.claude/settings.json`'s build hook tests `*.sln *.slnx` — so the checker was the
outlier. `has_solution()` now matches both, `.slnf` is deliberately excluded (a filter over a
solution, not one), and rules 4/6/9, `verify-config`'s step 6 and `.ai/tests/README.md` now read
"a solution file (`*.sln` or `*.slnx`)". **Recorded gap, not fixed:** no committed test fails if
that tuple is reverted — every suite invocation of this validator runs the no-solution path, and a
faithful fixture would need real-manifest machinery. Its own task if a guard is wanted.

## Implementation notes for artifact 9 (`validate-traceability.py`)

- **T5 must count distinct tags, not occurrences.** A scenario and its tag exist in *two* copies —
  the Gherkin block in `user-stories.md` and the `.feature` under `docs/slices/`. Counting
  occurrences reports two scenarios per covered criterion. (Nothing checks the copies *agree*; noted
  as a residual gap.)
- **T2 reads `.feature` files only.**
- **T6 accepts either** `[TestCategory("AC-…")]` **or** an executable `@AC-`-tagged scenario.
- **T7 extends to `use-cases.md`**, not just story files.
- Legacy blueprint tolerance: read singular `use_case`/`user_story` and the old top-level free-text
  `acceptance_criteria` as one-element arrays.
